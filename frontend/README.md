# FitCoach Pro Frontend

React/Vite frontend for FitCoach Pro.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run test
npm run build
```

The app runs on `http://localhost:5173` and proxies `/api` requests to the Flask backend on `http://localhost:5000`.
